# 技术架构详细说明

## 📐 系统架构概览

DeepTalk项目采用前后端分离架构,使用现代Web技术栈构建。

```
┌─────────────────────────────────────────────────────────┐
│                     浏览器 (Browser)                      │
├─────────────────────────────────────────────────────────┤
│                   前端应用 (Frontend)                     │
│    Vue 3 + TypeScript + Ant Design Vue + Vite          │
├─────────────────────────────────────────────────────────┤
│                    HTTP/HTTPS                           │
├─────────────────────────────────────────────────────────┤
│                   后端API (Backend)                      │
│         Spring Boot + Spring Data JPA                   │
├─────────────────────────────────────────────────────────┤
│                   数据库 (Database)                      │
│                     MySQL 8.0                           │
└─────────────────────────────────────────────────────────┘
```

---

## 🎨 前端技术架构

### 技术栈

#### 核心框架
- **Vue 3.3+**: 渐进式JavaScript框架
  - Composition API: 更好的逻辑复用
  - Reactivity System: 响应式数据系统
  - Virtual DOM: 高效的DOM更新

#### 开发语言
- **TypeScript 5.0+**: 类型安全的JavaScript超集
  - 静态类型检查
  - 接口和类型定义
  - IDE智能提示
  - 代码重构支持

#### UI组件库
- **Ant Design Vue 4.0+**: 企业级UI组件库
  - 丰富的组件库
  - 主题定制
  - 国际化支持
  - 响应式设计

#### 构建工具
- **Vite 5.0+**: 下一代前端构建工具
  - 极速的开发服务器
  - 热模块替换(HMR)
  - 优化的生产构建
  - 插件生态系统

#### 路由管理
- **Vue Router 4**: Vue官方路由管理器
  - 声明式路由
  - 路由懒加载
  - 导航守卫
  - 路由参数

#### HTTP客户端
- **Axios 1.6+**: Promise based HTTP client
  - 拦截器
  - 请求/响应转换
  - 取消请求
  - 超时处理

### 项目结构

```
frontend/src/
├── api/                    # API调用层
│   ├── CommunityAPI.ts    # 社区相关API
│   ├── ShopAPI.ts         # 商店相关API
│   └── index.ts           # API统一导出
│
├── components/             # 可复用组件
│   ├── Navbar.vue         # 导航栏
│   ├── PostPreview.vue    # 帖子预览卡片
│   └── ProductCard.vue    # 商品卡片
│
├── views/                  # 页面视图
│   ├── Home.vue           # 主页
│   ├── Community.vue      # 社区页面
│   ├── PostDetail.vue     # 帖子详情
│   └── Shop.vue           # 商店页面
│
├── controllers/            # 业务控制器
│   └── CommunityController.ts  # 社区控制器
│
├── interface/              # TypeScript接口
│   ├── CommunityInterface.ts   # 社区接口定义
│   └── ShopInterface.ts        # 商店接口定义
│
├── composables/            # 组合式函数
│   ├── useAuth.ts         # 认证逻辑
│   └── usePagination.ts   # 分页逻辑
│
├── utils/                  # 工具函数
│   ├── request.ts         # 请求封装
│   ├── storage.ts         # 本地存储
│   └── validators.ts      # 验证器
│
├── router/                 # 路由配置
│   └── index.ts           # 路由定义
│
├── types/                  # 类型定义
│   └── global.d.ts        # 全局类型
│
├── App.vue                 # 根组件
└── main.ts                 # 应用入口
```

### 架构层级

```
┌─────────────────────────────────────────┐
│         View Layer (视图层)              │
│         Vue Components                  │
├─────────────────────────────────────────┤
│      Controller Layer (控制器层)         │
│      Business Logic                     │
├─────────────────────────────────────────┤
│         API Layer (API层)               │
│         HTTP Requests                   │
├─────────────────────────────────────────┤
│      Utils Layer (工具层)               │
│      Helper Functions                   │
└─────────────────────────────────────────┘
```

### 数据流

```
用户交互 → Vue Component → Controller → API → Backend
                ↓                              ↓
            更新视图 ← 响应式数据 ← 处理响应 ←─┘
```

### 关键技术实现

#### 1. API封装
```typescript
// api/request.ts - Axios配置
import axios from 'axios';

const request = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL,
  timeout: 10000,
});

// 请求拦截器
request.interceptors.request.use(
  (config) => {
    const token = localStorage.getItem('token');
    if (token) {
      config.headers.Authorization = `Bearer ${token}`;
    }
    return config;
  },
  (error) => Promise.reject(error)
);

// 响应拦截器
request.interceptors.response.use(
  (response) => response.data,
  (error) => {
    // 统一错误处理
    return Promise.reject(error);
  }
);
```

#### 2. 组合式函数
```typescript
// composables/usePagination.ts
export function usePagination(fetchFn: Function) {
  const page = ref(1);
  const pageSize = ref(10);
  const total = ref(0);
  const loading = ref(false);
  const data = ref([]);

  const loadData = async () => {
    loading.value = true;
    try {
      const res = await fetchFn(page.value, pageSize.value);
      data.value = res.data;
      total.value = res.total;
    } finally {
      loading.value = false;
    }
  };

  return { page, pageSize, total, loading, data, loadData };
}
```

#### 3. 路由配置
```typescript
// router/index.ts
const routes = [
  {
    path: '/',
    name: 'Home',
    component: () => import('@/views/Home.vue'),
    meta: { requiresAuth: true }
  },
  {
    path: '/community',
    name: 'Community',
    component: () => import('@/views/Community.vue'),
    meta: { requiresAuth: true }
  },
  // ... 其他路由
];

// 路由守卫
router.beforeEach((to, from, next) => {
  if (to.meta.requiresAuth && !isLoggedIn()) {
    next('/login');
  } else {
    next();
  }
});
```

---

## 🔧 后端技术架构

### 技术栈

#### 核心框架
- **Spring Boot 3.1+**: Java企业级应用框架
  - 自动配置
  - 嵌入式服务器
  - 生产就绪特性
  - 微服务支持

#### 数据访问
- **Spring Data JPA**: 简化数据访问层
  - 自动实现Repository
  - 查询方法命名约定
  - 分页和排序
  - 事务管理

#### ORM框架
- **Hibernate**: 对象关系映射
  - 实体映射
  - 懒加载
  - 缓存机制
  - HQL查询

#### 数据库
- **MySQL 8.0**: 关系型数据库
  - ACID事务
  - 索引优化
  - 外键约束
  - 视图和存储过程

### 项目结构

```
backend/src/main/java/com/example/deeptalk/
└── modules/
    ├── community/              # 社区模块
    │   ├── controller/        # 控制器层
    │   │   └── CommunityController.java
    │   ├── service/           # 服务层
    │   │   ├── CommunityService.java
    │   │   └── CommunityServiceImpl.java
    │   ├── repository/        # 数据访问层
    │   │   └── PostRepository.java
    │   └── entity/            # 实体类
    │       ├── Post.java
    │       └── Comment.java
    │
    └── shop/                   # 商店模块
        ├── controller/
        │   └── ShopController.java
        ├── service/
        │   ├── ShopService.java
        │   └── ShopServiceImpl.java
        ├── repository/
        │   └── ProductRepository.java
        └── entity/
            └── Product.java
```

### 架构层级

```
┌─────────────────────────────────────────┐
│     Controller Layer (控制器层)          │
│     RESTful API Endpoints               │
├─────────────────────────────────────────┤
│      Service Layer (服务层)             │
│      Business Logic                     │
├─────────────────────────────────────────┤
│    Repository Layer (数据访问层)         │
│    Database Operations                  │
├─────────────────────────────────────────┤
│      Entity Layer (实体层)              │
│      Domain Models                      │
└─────────────────────────────────────────┘
```

### 关键技术实现

#### 1. Controller层
```java
@RestController
@RequestMapping("/api/community")
@CrossOrigin(origins = "*")
public class CommunityController {
    
    @Autowired
    private CommunityService communityService;
    
    @GetMapping("/posts")
    public ResponseEntity<PageResult<Post>> getPosts(
        @RequestParam(defaultValue = "1") int page,
        @RequestParam(defaultValue = "10") int size
    ) {
        PageResult<Post> result = communityService.getPosts(page, size);
        return ResponseEntity.ok(result);
    }
    
    @PostMapping("/posts")
    public ResponseEntity<Post> createPost(@RequestBody Post post) {
        Post created = communityService.createPost(post);
        return ResponseEntity.ok(created);
    }
    
    @PostMapping("/posts/{id}/like")
    public ResponseEntity<Void> likePost(@PathVariable Long id) {
        communityService.likePost(id);
        return ResponseEntity.ok().build();
    }
}
```

#### 2. Service层
```java
@Service
public class CommunityServiceImpl implements CommunityService {
    
    @Autowired
    private PostRepository postRepository;
    
    @Override
    @Transactional(readOnly = true)
    public PageResult<Post> getPosts(int page, int size) {
        Pageable pageable = PageRequest.of(page - 1, size, 
            Sort.by("createdAt").descending());
        Page<Post> postPage = postRepository.findAll(pageable);
        return new PageResult<>(
            postPage.getContent(),
            postPage.getTotalElements(),
            page,
            size
        );
    }
    
    @Override
    @Transactional
    public Post createPost(Post post) {
        post.setCreatedAt(LocalDateTime.now());
        return postRepository.save(post);
    }
    
    @Override
    @Transactional
    public void likePost(Long postId) {
        Post post = postRepository.findById(postId)
            .orElseThrow(() -> new ResourceNotFoundException("Post not found"));
        post.setLikes(post.getLikes() + 1);
        postRepository.save(post);
    }
}
```

#### 3. Repository层
```java
@Repository
public interface PostRepository extends JpaRepository<Post, Long> {
    
    // 根据标题搜索
    List<Post> findByTitleContaining(String keyword);
    
    // 根据作者查询
    List<Post> findByAuthorId(Long authorId);
    
    // 查询热门帖子
    @Query("SELECT p FROM Post p WHERE p.likes > :minLikes ORDER BY p.likes DESC")
    List<Post> findHotPosts(@Param("minLikes") int minLikes);
    
    // 分页查询
    Page<Post> findAll(Pageable pageable);
}
```

#### 4. Entity层
```java
@Entity
@Table(name = "posts")
public class Post {
    
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;
    
    @Column(nullable = false, length = 200)
    private String title;
    
    @Column(columnDefinition = "TEXT")
    private String content;
    
    @ManyToOne(fetch = FetchType.LAZY)
    @JoinColumn(name = "author_id")
    private User author;
    
    @Column(name = "created_at")
    private LocalDateTime createdAt;
    
    @Column(nullable = false)
    private Integer likes = 0;
    
    @OneToMany(mappedBy = "post", cascade = CascadeType.ALL)
    private List<Comment> comments = new ArrayList<>();
    
    // Getters and Setters...
}
```

---

## 🗄️ 数据库设计

### 核心表结构

#### Posts表 (帖子)
```sql
CREATE TABLE posts (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    title VARCHAR(200) NOT NULL,
    content TEXT,
    author_id BIGINT NOT NULL,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME ON UPDATE CURRENT_TIMESTAMP,
    likes INT DEFAULT 0,
    views INT DEFAULT 0,
    status ENUM('active', 'deleted') DEFAULT 'active',
    FOREIGN KEY (author_id) REFERENCES users(id),
    INDEX idx_author (author_id),
    INDEX idx_created (created_at),
    INDEX idx_likes (likes)
);
```

#### Products表 (商品)
```sql
CREATE TABLE products (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(200) NOT NULL,
    description TEXT,
    price DECIMAL(10, 2) NOT NULL,
    image_url VARCHAR(500),
    category VARCHAR(50),
    stock INT DEFAULT 0,
    sales INT DEFAULT 0,
    rating DECIMAL(3, 2),
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    status ENUM('active', 'inactive') DEFAULT 'active',
    INDEX idx_category (category),
    INDEX idx_price (price),
    INDEX idx_rating (rating)
);
```

#### Comments表 (评论)
```sql
CREATE TABLE comments (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    post_id BIGINT NOT NULL,
    user_id BIGINT NOT NULL,
    content TEXT NOT NULL,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    likes INT DEFAULT 0,
    FOREIGN KEY (post_id) REFERENCES posts(id) ON DELETE CASCADE,
    FOREIGN KEY (user_id) REFERENCES users(id),
    INDEX idx_post (post_id),
    INDEX idx_created (created_at)
);
```

### 数据库优化

#### 索引策略
- 主键索引: 所有表的id字段
- 外键索引: 关联字段建立索引
- 查询索引: 常用查询字段建立索引
- 组合索引: 多字段联合查询

#### 查询优化
- 避免SELECT *
- 使用分页查询
- 合理使用JOIN
- 利用覆盖索引

---

## 🔐 安全架构

### 认证和授权

#### JWT令牌
```
┌─────────────┐
│   Client    │
└──────┬──────┘
       │ 1. Login (username, password)
       ↓
┌─────────────┐
│   Server    │
└──────┬──────┘
       │ 2. Verify & Generate JWT
       ↓
┌─────────────┐
│   Client    │ 3. Store JWT
└──────┬──────┘
       │ 4. Request with JWT in Header
       ↓
┌─────────────┐
│   Server    │ 5. Verify JWT & Process
└─────────────┘
```

### 数据安全
- **密码加密**: BCrypt加密存储
- **SQL注入防护**: 参数化查询
- **XSS防护**: 输入验证和转义
- **CSRF防护**: Token验证

---

## 📡 API设计规范

### RESTful API设计

#### 资源命名
- 使用名词复数形式
- 使用小写字母
- 使用连字符分隔

#### HTTP方法
- `GET`: 查询资源
- `POST`: 创建资源
- `PUT`: 更新资源(完整)
- `PATCH`: 更新资源(部分)
- `DELETE`: 删除资源

#### 状态码
- `200 OK`: 成功
- `201 Created`: 创建成功
- `400 Bad Request`: 请求错误
- `401 Unauthorized`: 未授权
- `404 Not Found`: 资源不存在
- `500 Internal Server Error`: 服务器错误

#### 响应格式
```json
{
  "code": 200,
  "message": "Success",
  "data": {
    // 数据内容
  },
  "timestamp": "2025-10-28T10:00:00Z"
}
```

---

## 🚀 部署架构

### 开发环境
```
Frontend: http://localhost:5173 (Vite Dev Server)
Backend:  http://localhost:8080 (Spring Boot)
Database: localhost:3306 (MySQL)
```

### 生产环境
```
┌─────────────────┐
│   Nginx/CDN     │ (静态资源)
├─────────────────┤
│   Frontend      │ (Vue Build)
└────────┬────────┘
         │
┌────────▼────────┐
│   API Gateway   │ (反向代理)
├─────────────────┤
│   Backend       │ (Spring Boot)
├─────────────────┤
│   Database      │ (MySQL Master/Slave)
└─────────────────┘
```

---

## 📊 性能优化策略

### 前端优化
- **代码分割**: 路由懒加载
- **资源压缩**: Gzip/Brotli
- **缓存策略**: HTTP缓存、本地存储
- **图片优化**: 懒加载、WebP格式

### 后端优化
- **数据库连接池**: HikariCP
- **查询优化**: 索引、缓存
- **异步处理**: @Async注解
- **缓存机制**: Redis缓存

---

**文档创建时间**: 2025年10月28日  
**技术版本**: Vue 3.3+ / Spring Boot 3.1+ / MySQL 8.0
